# Локальные файлы для превью в Claude Code — на сайт не нужны
.claude/
.DS_Store

# Финансовый отчёт с реальными суммами — НЕ выгружать в публичный репозиторий
finances.html

# База бэкенда — не коммитить (пользовательские данные)
server/data.db
server/data.db-*
